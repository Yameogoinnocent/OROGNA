@extends('layouts.admin')
@section('title','Paramètres — Administration')
@section('content')<div class="mx-auto max-w-5xl p-5 lg:p-10"><div><div class="text-xs font-black uppercase tracking-widest text-orange-500">Configuration</div><h1 class="font-display mt-2 text-3xl font-extrabold">Paramètres du site</h1><p class="mt-2 text-slate-500">Modifiez les textes, coordonnées, réseaux sociaux et paramètres de localisation sans toucher au code.</p></div><form method="POST" action="{{ route('admin.settings.update') }}" enctype="multipart/form-data" class="mt-8 space-y-6">@csrf @method('PUT')
<section class="rounded-3xl bg-white p-7 shadow-sm ring-1 ring-slate-100"><div class="text-xs font-black uppercase tracking-widest text-green-700">Identité</div><h2 class="font-display mt-1 text-xl font-extrabold">Logo & apparence</h2><div class="mt-5 grid gap-5 md:grid-cols-2"><div><label class="mb-2 block text-xs font-black uppercase tracking-wider text-slate-400">Logo du site</label><input class="field" type="file" name="logo_upload" accept="image/jpeg,image/png,image/webp"><p class="mt-2 text-xs text-slate-400">Le logo sera utilisé dans le site et les espaces publics.</p>@if($settings['logo']??null)<img src="{{ asset($settings['logo']->value) }}" class="mt-3 h-16 rounded-xl bg-slate-50 object-contain p-2" alt="Logo">@endif</div><div><label class="mb-2 block text-xs font-black uppercase tracking-wider text-slate-400">Favicon</label><input class="field" type="file" name="favicon_upload" accept="image/jpeg,image/png,image/webp,image/x-icon"><p class="mt-2 text-xs text-slate-400">Petite icône affichée dans l’onglet du navigateur.</p></div></div></section>
<section class="rounded-3xl bg-white p-7 shadow-sm ring-1 ring-slate-100"><div class="text-xs font-black uppercase tracking-widest text-orange-500">Coordonnées & textes</div><h2 class="font-display mt-1 text-xl font-extrabold">Informations générales</h2><div class="mt-5 grid gap-5">@foreach($settings as $key=>$setting)@continue(in_array($key,['logo','favicon']))<div><label class="mb-2 block text-xs font-black uppercase tracking-wider text-slate-400">{{ str_replace('_',' ',$key) }}</label><textarea class="field" name="settings[{{ $key }}]" rows="{{ str_contains($key,'text')||str_contains($key,'title')||str_contains($key,'tagline') ? 3 : 2 }}">{{ $setting->value }}</textarea></div>@endforeach</div></section>
<div class="flex justify-end"><button class="or-btn or-btn-orange">Enregistrer les paramètres ↗</button></div></form></div>@endsection
