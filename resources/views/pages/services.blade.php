@extends('layouts.app')
@section('title','Nos expertises — OROGNA Consulting')
@section('content')
<section class="section-cream py-24"><div class="container-pro"><span class="eyebrow">Nos expertises</span><h1 class="font-display mt-5 max-w-4xl text-5xl font-extrabold tracking-tight sm:text-6xl">Une équipe pluridisciplinaire pour transformer vos enjeux en solutions.</h1><p class="mt-7 max-w-3xl text-lg leading-8 text-slate-600">Découvrez les domaines d’activité d’OROGNA Consulting, de l’acquisition de talents à la transformation digitale.</p></div></section>
<section class="bg-white py-20"><div class="container-pro"><div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">@foreach(\App\Models\Service::where('is_active',true)->orderBy('sort_order')->get() as $i=>$service)<article class="card-pro group overflow-hidden"><div class="relative h-56 overflow-hidden bg-slate-100">@if($service->image)<img src="{{ asset($service->image) }}" class="h-full w-full object-cover transition duration-700 group-hover:scale-105" alt="{{ $service->title }}">@endif<div class="absolute inset-0 bg-gradient-to-t from-[#10281d]/75 to-transparent"></div><span class="absolute bottom-5 left-5 rounded-full bg-white/95 px-4 py-2 text-xs font-black">{{ str_pad($i+1,2,'0',STR_PAD_LEFT) }}</span></div><div class="p-7"><h2 class="font-display text-2xl font-extrabold">{{ $service->title }}</h2><p class="mt-4 text-sm leading-7 text-slate-500">{{ $service->short_description }}</p></div></article>@endforeach</div></div></section>
@endsection
