@extends('layouts.app')
@section('title','Formations — OROGNA Consulting')
@section('content')
<section class="section-cream py-24"><div class="container-pro"><span class="eyebrow">Académie OROGNA</span><h1 class="font-display mt-5 max-w-4xl text-5xl font-extrabold sm:text-6xl">Des formations conçues pour être utiles dès demain.</h1><p class="mt-6 max-w-2xl text-lg leading-8 text-slate-600">Développez les compétences de vos équipes avec des parcours pratiques, contextualisés et animés par des professionnels.</p></div></section>
<section class="bg-white py-20"><div class="container-pro grid gap-6 md:grid-cols-2 lg:grid-cols-3">@foreach($trainings as $training)<article class="card-pro overflow-hidden"><div class="h-48 overflow-hidden"><img src="{{ asset($training->image ?: 'images/team.jpg') }}" class="h-full w-full object-cover" alt="{{ $training->title }}"></div><div class="p-7"><div class="flex items-center justify-between text-xs font-black uppercase tracking-wider"><span class="text-orange-500">{{ optional($training->start_date)->translatedFormat('d M Y') }}</span><span class="text-slate-400">{{ $training->duration }}</span></div><h2 class="font-display mt-4 text-2xl font-extrabold">{{ $training->title }}</h2><p class="mt-3 text-sm leading-7 text-slate-500">{{ $training->excerpt }}</p><a class="mt-6 inline-flex font-extrabold text-green-700" href="{{ route('trainings.show',$training) }}">Voir le programme →</a></div></article>@endforeach</div><div class="container-pro mt-10">{{ $trainings->links() }}</div></section>
@endsection
