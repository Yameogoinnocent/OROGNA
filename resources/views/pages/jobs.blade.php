@extends('layouts.app')
@section('title','Opportunités — OROGNA Consulting')
@section('content')
<section class="hero-grid py-24 text-white"><div class="container-pro"><span class="eyebrow text-orange-300">Carrières</span><h1 class="font-display mt-5 max-w-4xl text-5xl font-extrabold sm:text-6xl">Votre prochaine opportunité peut commencer ici.</h1><p class="mt-6 max-w-2xl text-lg leading-8 text-white/65">Découvrez nos postes ouverts et envoyez votre candidature en quelques minutes.</p></div></section>
<section class="bg-white py-20"><div class="container-pro"><div class="grid gap-5">@forelse($offers as $job)<a href="{{ route('jobs.show',$job) }}" class="card-pro group grid gap-7 p-7 md:grid-cols-[1fr_auto] md:items-center"><div><div class="flex flex-wrap gap-2"><span class="rounded-full bg-green-50 px-3 py-1 text-xs font-black text-green-700">{{ $job->sector ?: 'Conseil' }}</span><span class="rounded-full bg-orange-50 px-3 py-1 text-xs font-black text-orange-600">{{ $job->location }}</span></div><h2 class="font-display mt-4 text-2xl font-extrabold">{{ $job->title }}</h2><p class="mt-2 max-w-2xl text-sm leading-7 text-slate-500">{{ $job->short_description }}</p><div class="mt-5 text-xs font-bold uppercase tracking-wider text-slate-400">Réf. {{ $job->reference }} · {{ $job->contract_type }}</div></div><span class="rounded-full bg-slate-950 px-5 py-3 font-extrabold text-white transition group-hover:bg-green-700">Postuler ↗</span></a>@empty<div class="rounded-3xl bg-slate-50 p-12 text-center"><div class="text-4xl">✦</div><h2 class="mt-4 text-2xl font-extrabold">Aucune offre ouverte</h2><p class="mt-2 text-slate-500">Vous pouvez tout de même déposer une candidature spontanée.</p><a class="btn-primary mt-6" href="{{ route('apply') }}">Déposer mon CV</a></div>@endforelse</div></div></section>
@endsection
